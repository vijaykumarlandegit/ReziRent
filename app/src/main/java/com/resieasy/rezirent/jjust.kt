package com.resieasy.rezirent

/*

 if (subtype.isNullOrEmpty()) {
        Toast.makeText(context, "Subtype cannot be null or empty!", Toast.LENGTH_SHORT).show()
    } else {
        sendnotandlead(userid!!, id, subtype)
    }

if (someInt == null || someInt == 0) {
    Toast.makeText(context, "Value cannot be null or zero!", Toast.LENGTH_SHORT).show()
} else {
    someFunction(someInt)
}

if (someDouble == null || someDouble == 0.0) {
    Toast.makeText(context, "Value cannot be null or zero!", Toast.LENGTH_SHORT).show()
} else {
    someFunction(someDouble)
}

if (someLong == null || someLong == 0L) {
    Toast.makeText(context, "Value cannot be null or zero!", Toast.LENGTH_SHORT).show()
} else {
    someFunction(someLong)
}
 val remotimage: MutableList<SlideModel> = ArrayList()
 private val imageUrlMap = mutableMapOf<String, String>() // Cache for images


 if (data != null) {
            val idd = data.id

            if (!idd.isNullOrEmpty()) {
                println("d")
            } else {
                println("s")
            }
        } else {
            // Handle the case where data is null
        }
*/
